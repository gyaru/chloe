# Discord Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# Database Configuration
POSTGRES_URL=postgresql://username:password@localhost:5432/chloe_db
REDIS_URL=redis://localhost:6379

# LLM Provider Configuration
# At least one of these API keys is required:

# Groq API Key (for Groq provider)
GROQ_API_KEY=your_groq_api_key_here

# z.AI API Key (for z.AI provider)
ZAI_API_KEY=your_zai_api_key_here

# OpenRouter API Key (for OpenRouter provider)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: Force specific LLM provider
# If not set, the bot will auto-select based on available API keys (priority: OpenRouter > z.AI > Groq)
# Supported values: "groq", "zai", "openrouter"
# LLM_PROVIDER=openrouter

# Optional: Specific model to use with any provider
# Each provider has its own default if not specified:
# - Groq: moonshotai/kimi-k2-instruct-0905
# - z.AI: GLM-4.5
# - OpenRouter: openai/gpt-4o-mini

# Examples of LLM_MODEL usage:
# LLM_MODEL=anthropic/claude-3.5-sonnet    # High-performance (OpenRouter)
# LLM_MODEL=openai/gpt-4o-mini             # Cost-effective (OpenRouter/Groq)
# LLM_MODEL=GLM-4.5V                       # Vision support (z.AI)
# LLM_MODEL=x-ai/grok-code-fast-1          # Coding specialist (OpenRouter)
# LLM_MODEL=llama-3.3-70b-versatile        # Fast inference (Groq)

# Optional: Web Search API Key
# Required for web search functionality
EXA_KEY=your_exa_search_api_key_here

# Image Generation Configuration (Coming Soon)
# IMAGE_GEN_PROVIDER=openrouter
# IMAGE_GEN_MODEL=black-forest-labs/flux-1-dev

# Optional: Logging Configuration
RUST_LOG=chloe=info,warn
